<script>

$(function() {
  $('a[href*=#]:not([href=#])').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
      var target = $(this.hash);
      target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
      if (target.length) {
        $('html,body').animate({
          scrollTop: target.offset().top
        }, 1500, 'easeInOutExpo');
        return false;
      }
    }
  });
});

</script>

<div id="header-div">
  	<h1>Welcome.</h1>
  	<p>What is a Super Awesome To-Do list?</p>
	<p>A list that helps you get stuff done.</p>
	<p>And help others get stuff done too.</p>
	<%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-large btn-primary" %>
	<a href="#video" class = "btn btn-large btn-primary">Find Out More</a>
</div>

<a id="video"></a>
<div id="content-div">
  <video class = "video" autoplay loop = "1">
    <source src = "video/videoplayback.mp4">
  </video>
</div>

