added a new task: 
<% if activity.trackable.private %>
	that is private.

<% elsif activity.trackable %>
	<%= link_to activity.trackable.title, activity.trackable %>

<% else %>
	that has since been removed.
<% end %>