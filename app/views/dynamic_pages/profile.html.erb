<% provide(:title, @user.username) %>
<div class = "row">

	<div class = "col-sm-1"></div>

	<div class = "col-sm-2">

		<h1><%= @user.username %></h1>
		<p><%= @user.email %></p>
		<%= render 'shared/stats' %>
		<h2>Add Task</h2>
		<%= render 'shared/task_form' %>

	</div>
	
	<div class = "col-sm-6">

		<h2>To Do</h2>
		<%= render 'shared/todo_part' %>
		<h2>Done Items</h2>
		<%= render 'shared/done_part' %>

	</div>
	
	<div class = "col-sm-3">

		<h2>Activity Feed</h2>
		<!-- Uncomment below to inspect what the activity feed pushes out -->
		<!-- <% @activities.each do |activity| %>
			<%= activity.inspect %>
		<% end %> -->

		<%= render 'shared/activity_feed' %>

	</div>


</div>
