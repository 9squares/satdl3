<!-- This part loads the entry form to create new task for the user -->
<div>
	<%= form_for(@task) do |f| %>
		<div><%= f.label :title %><br>
			<%= f.text_area :title, placehodler: "Create new task...", class: "form-control" %>
		</div>
		<%= f.submit "Add", class: "btn btn-primary pull-right" %>
	<% end %>
</div>