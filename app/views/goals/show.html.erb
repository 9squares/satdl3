<% provide(:title, @goal.title) %>
<div class="container">
	<h1><%= @goal.title %></h1>
	<p>Created on <%= @goal.created_at.to_date %></p>
	<%= render 'shared/goalstask_form' %>
</div>
<div class="container">

	<h2>To Do</h2>
	<ul class="nav nav-tabs" id="myTab">
	  <li><a href="#today" data-toggle="tab">Today</a></li>
	  <li><a href="#week" data-toggle="tab">This Week</a></li>
	  <li><a href="#month" data-toggle="tab">This Month</a></li>
	  <li><a href="#year" data-toggle="tab">This Year</a></li>
	  <li><a href="#5years" data-toggle="tab">5 Years</a></li>
	  <li class="active"><a href="#all" data-toggle="tab">All</a></li>
	</ul>
	
	<div class="tab-content">
	  <div class="tab-pane fade in" id="today">
	  	<%= render 'shared/todo_part_today' %>
	  </div>
	  <div class="tab-pane fade" id="week">
	  	<%= render 'shared/todo_part_week' %>
	  </div>
	  <div class="tab-pane fade" id="month">
	  	<%= render 'shared/todo_part_month' %>
	  </div>
	  <div class="tab-pane fade" id="year">
	  	<%= render 'shared/todo_part_year' %>
	  </div>
	  <div class="tab-pane fade" id="5years">
	  	<%= render 'shared/todo_part_5years' %>
	  </div>
	  <div class="tab-pane fade in active" id="all">
	  	<%= render 'shared/todo_part' %>
	  </div>
	</div>
	
	<h2>Items Done Today</h2>
	<%= render 'shared/done_part' %>

</div>